#--------------------------------------------------------------------------------------------------
#--------------------------------------------------------------------------------------------------

# # TODO: check for rpart also
# checkParams = function(big.mat) {

#   colnames(big.mat) = c("C", "M", "N", "O", "R", "B", "S", "A", "J", "valid.acc", "test.acc", 
#     "train.time", "num.leaves", "tree.size")

#   big.df = data.frame(big.mat)
#   # R = 0, N = NA, else C = NA
#   ids = which(big.df$R == 0)
#   big.df[ ids, "N"] = 0
#   big.df[-ids, "C"] = 0

#   return(big.df)
# }

#--------------------------------------------------------------------------------------------------
#--------------------------------------------------------------------------------------------------

checkSubdir = function(algo, subdir) {

  my.path = paste0("data/", algo, "/", subdir)
  v1 = dir.exists(path = my.path)
  v2 = (length(list.files(path = my.path)) != 0)
  return(v1 & v2)

}

#--------------------------------------------------------------------------------------------------
#--------------------------------------------------------------------------------------------------
